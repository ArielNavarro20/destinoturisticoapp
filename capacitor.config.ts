import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.destinosturisticosapp',
  appName: 'destinosturisticosapp',
  webDir: 'www'
};

export default config;
